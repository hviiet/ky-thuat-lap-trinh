#include <stdio.h>
long long giaithua(int n)
{
    double s = 1;
    for (int j = 2; j <= i; j++)
        s = s * j;
    return s;
}
int main()
{
    int k, n;
    scanf("%d %d", &k, &n);
    if (k > n - k)
        k = n - k;
    double x = 1;
    for (int i = n; i >= n - k; i--)
    {
    }
    return 0;
}